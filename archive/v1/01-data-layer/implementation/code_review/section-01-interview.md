# Review Interview: Section 01 — Foundation

## Auto-fixes Applied (no user input needed)
1. Added explanation comment to `ConnectionType` documenting the ASCII `->` decision
2. Added `"lib": ["ES2022"]` to tsconfig.json to prevent DOM global ambiguity

## Let Go
- TypeScript version pinning — preference, not a correctness issue for a private tool

## No user questions — all items were clear auto-fixes or non-issues
