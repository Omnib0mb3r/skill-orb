node_modules/
dist/
.env
*.log
*.vsix
.superpowers/

# Personal screenshots (chat-history captures, design references)
Pics/

# DevNeural data root if symlinked or co-located
skill-connections/
*.tsbuildinfo
.next/

# Playwright dev artifacts (exploration screenshots, MCP traces)
.playwright-mcp/
/orb-*.png
/home-*.png

# Claude Code local-only state. .claude/commands/ stays tracked (shared
# slash commands); these two files are per-machine and must not ship.
/.claude/scheduled_tasks.lock
/.claude/settings.local.json
